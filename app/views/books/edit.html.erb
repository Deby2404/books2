<h1>Editing Book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<%= form_with model: @book do |f| %>
 <label for="inputText">Title</label><br>
 <%= f.text_field :title, autofocus: true, placeholder:"title" %><br>

 <label for="inputText">Opinion</label><br>
 <%= f.text_area :body, placeholder: "introduction" %><br>
 <%= f.submit "Update Book", class: "btn btn-success" %><br>

 <%= link_to "Show", book_path(@book) %>
  | <%= link_to "Back", books_path %>
 <% end %>


